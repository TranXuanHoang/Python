{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Blockchain",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}/01-blockchain",
            "console": "integratedTerminal"
        }
    ]
}